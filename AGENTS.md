# AGENTS.md

## Setup commands
- Install deps: `uv sync`
- Start scripts: `uv run <script>`

## Code style
- Use type hints where reasonably possible
- don't overly use try/except. It's fine if things fail sometimes.
